import { LucideIcon, Check, X, NotebookText, Settings2, BookUser, User, CircleCheck, CircleX, Home, LineChart } from "lucide-react";

export type Icon = LucideIcon

export const Icons = {
  check: Check,
  close: X,
  course: NotebookText,
  settings: Settings2,
  user: User,
  checkCircle: CircleCheck,
  xCircle: CircleX,
  home: Home,
  statistic: LineChart
}